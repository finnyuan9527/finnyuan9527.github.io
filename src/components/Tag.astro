---
interface Props {
  label: string;
}
const { label } = Astro.props;
---

<span class="inline-flex items-center rounded-full bg-[var(--color-accent)]/10 px-3 py-1 text-xs font-medium text-[var(--color-accent)]">
  {label}
</span>
